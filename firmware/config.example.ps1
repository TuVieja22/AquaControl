# Copia este archivo como "config.local.ps1" (no se sube a GitHub) y completalo.
# Lo lee firmware\puente_usb.ps1.

# API key del dispositivo: en la pagina, Dispositivos > "Generar key".
$API_KEY = 'aqk_...'

# Opcionales (estos son los valores por defecto):
# $PORT = 'auto'                   # o 'COM4'
# $BASE_URL = 'http://127.0.0.1:8080'
# $INTERVALO_LECTURA_S = 5
# $INTERVALO_ORDENES_S = 2
